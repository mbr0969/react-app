import React from 'react';
import classes  from './Header.module.css';

const Header  = () =>{
  return(
    <header className={classes.header}>
    <img src="logoza.ru.png" alt='' ></img>
  </header>
  );
}
export default Header;